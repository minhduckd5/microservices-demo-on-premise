apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: microservices
  labels:
    app.kubernetes.io/name: orders
spec:
  selector:
    app.kubernetes.io/name: orders
  ports:
    - port: 8003
      targetPort: 8003
      protocol: TCP
  type: ClusterIP
