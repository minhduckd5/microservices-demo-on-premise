apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: microservices
  labels:
    app.kubernetes.io/name: auth
spec:
  selector:
    app.kubernetes.io/name: auth
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
  type: ClusterIP
